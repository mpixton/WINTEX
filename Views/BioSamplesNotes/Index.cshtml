@model IEnumerable<WINTEX.Models.BioSamplesNote>



<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NoteBody)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BioSample)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NoteBody)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BioSample.BioSampleId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BioNoteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BioNoteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BioNoteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
