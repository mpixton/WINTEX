@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model AddLocation
@using WINTEX.Enums

@Html.DropDownListFor(m => m.Subplot, new SelectList(CardinalDirections.SubPlots()), "Select a subplot")
@Html.DropDownListFor(m => m.East, new SelectList(CardinalDirections.EastWest()), "East or West?")
@Html.DropDownListFor(m => m.North, new SelectList(CardinalDirections.NorthSouth()), "North Or South?")
